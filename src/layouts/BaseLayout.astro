---
// Fuentes self-hosted vía JS import → Vite emite y reescribe las URLs de los .woff2
// (importarlas con @import dentro de global.css NO emite los archivos → 404 en producción).
import '@fontsource/instrument-serif/400.css';
import '@fontsource/instrument-serif/400-italic.css';
import '@fontsource/schibsted-grotesk/400.css';
import '@fontsource/schibsted-grotesk/500.css';
import '@fontsource/schibsted-grotesk/700.css';
import '@fontsource/ibm-plex-mono/400.css';
import '@fontsource/ibm-plex-mono/500.css';
import '~/styles/global.css';
import SEO from '@components/SEO.astro';
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';

interface Props {
  title: string;
  description: string;
  image?: string;
  ogType?: string;
  noindex?: boolean;
  bodyClass?: string;
  canonicalPath?: string;
  /** 'dark' = hero con foto oscura → header transparente con texto claro arriba.
      'light' = hero claro → header crema desde el inicio. Default 'dark'. */
  heroTheme?: 'dark' | 'light';
}

const {
  title,
  description,
  image,
  ogType = 'website',
  noindex = false,
  bodyClass = '',
  canonicalPath,
  heroTheme = 'dark',
} = Astro.props;

const GA_ID = import.meta.env.PUBLIC_GA_ID;
const CLARITY_ID = import.meta.env.PUBLIC_MICROSOFT_CLARITY_ID;
const GSC_VERIFICATION = import.meta.env.PUBLIC_GSC_VERIFICATION;
---

<!doctype html>
<html lang="es-DO">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <SEO
    title={title}
    description={description}
    image={image}
    ogType={ogType}
    noindex={noindex}
    canonicalPath={canonicalPath}
  />
  {GSC_VERIFICATION && <meta name="google-site-verification" content={GSC_VERIFICATION} />}

  {GA_ID && (
    <>
      <script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`}></script>
      <script define:vars={{ GA_ID }}>
        window.dataLayer = window.dataLayer || [];
        function gtag(){ dataLayer.push(arguments); }
        gtag('js', new Date());
        gtag('config', GA_ID);
      </script>
    </>
  )}

  {CLARITY_ID && (
    <script define:vars={{ CLARITY_ID }}>
      (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", CLARITY_ID);
    </script>
  )}

  <slot name="head" />
</head>
<body class={bodyClass} data-hero={heroTheme}>
  <a href="#main" class="skip-link">Saltar al contenido</a>
  <Header />
  <main id="main">
    <slot />
  </main>
  <Footer />
</body>
</html>
